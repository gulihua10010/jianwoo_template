<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.jianwoo.system.mapper.biz.SequenceBizMapper">
    <select id="selectNextSeq" resultType="java.lang.Long" parameterType="map">
        SELECT NEXT_SEQ(#{name,jdbcType=VARCHAR});
    </select>
</mapper>